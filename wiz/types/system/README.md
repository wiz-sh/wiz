# `@types/system`

Opt-in paths: `accounts`, `processes`, `services`, and `desktop`.

`accounts` covers Linux and Debian account administration including adduser,
useradd/usermod/userdel, group management, passwd/chage, getent, and related
identity tools. `services` includes the full sudo option surface.
