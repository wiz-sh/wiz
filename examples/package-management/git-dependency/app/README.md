# Git dependency consumer

Run the commands in the parent README after initializing the sibling `logger`
directory as a Git repository. Wiz writes the selected dependency to this
package's manifest and lockfile.
