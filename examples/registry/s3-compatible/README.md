# S3-compatible storage

The registry speaks the S3 API through a storage interface. This example uses MinIO locally; set
the same `S3_*` variables to use a managed compatible service. Keep the bucket private.
