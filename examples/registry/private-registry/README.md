# Private self-hosted registry

Expose the API only behind an HTTPS reverse proxy, keep dependency ports private, and publish
packages with private visibility. The example demonstrates restrictive CORS and JSON audit logs;
organization and package grants still provide fine-grained access.
