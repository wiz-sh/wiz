# Publishable package examples

These packages demonstrate public, private, organization, and mixed-source manifests. Every
manifest is parsed by the integration suite and uses the public JSON Schema.

```console
bun run example packages/public-package
```

Publishing examples require a configured registry identity and a scope owned by that identity.
