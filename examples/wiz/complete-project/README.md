# Complete project

Run `wiz check`, `wiz format --check .`, `wiz lint .`, and `wiz c build`.
Executing `bash dist/main.sh` prints `Wiz project: complete`; editor features resolve `banner`
across the sourced file.
